# アイケアLaBo四ツ谷店 GBP記事制作ルール

## 店舗情報
- 店舗名：アイケアLaBo四ツ谷店
- 所在地：新宿区四ツ谷駅から徒歩4分
- アクセス：市ヶ谷・麹町・曙橋からも好アクセス

## 記事を書く前に必ず参照すること
- references/tone-manner.md（文体・トンマナ）
- references/ng-words.md（禁止表現）
- references/template.md（記事構成テンプレート）

## ターゲット読者
- メイン：30〜50代の社会人・デスクワーカー
- サブ：目の疲れ・老眼・肩こりに悩む方、子どもの視力を心配する親御さん

## 文字数
- 900〜1,100字以内（GBP投稿の最適範囲）

## 必須ルール
- 冒頭は必ず定型文から始める
- 専門用語には必ず読み方を括弧で添える（例：毛様体筋（もうようたいきん））
- 末尾には必ず免責文を挿入する
- 記事はarticles/フォルダにMarkdownファイルで保存する

## 冒頭定型文
こんにちは！新宿区・四ツ谷駅から徒歩4分の「アイケアLaBo四ツ谷店」です👀✨

## 末尾免責文
⚠️ご注意
当店は医療機関ではありません。激しい痛みや異常、長期間続く症状などは、必ず整形外科や眼科等の専門家や医師の診断を受けてください。

---

## 作業ログ

### 2026-05-01〜05-04

#### 作成したファイル・ツール

| ファイル | 内容 |
|---|---|
| `articles/202605_夕方ピントが合わなくなる.md` | 初回生成記事（テーマ：夕方のピンぼけ／シーン起点／30〜40代） |
| `articles/202605_コンタクトの不快感、実は目の筋肉が原因かも.md` | 自動生成フローで生成した記事（ドライアイ／症状起点／30代） |
| `tools/notion_register.py` | Notionに記事タイトル・ステータス・ファイルパス・本文ブロックを登録するCLIツール |
| `tools/generate_article.py` | ネタストック取得→記事生成→保存→Notion登録→ステータス更新を一括実行するスクリプト |
| `references/past_themes.md` | 使用済みの導入パターン・例え話・構成を蓄積する差別化管理ファイル |
| `.github/workflows/generate_article.yml` | 毎月20日9時（JST）に記事生成を自動実行するGitHub Actionsワークフロー |

#### Notionデータベース構成

- **データベースID**：`3549bd0d2230802dadbacb89debcb624`
- **プロパティ**：名前（title）、ステータス（select）、テーマカテゴリ（select）、切り口パターン（select）、ターゲット読者（select）、生成記事リンク（url）、投稿日（date）
- **ステータスの運用フロー**：ネタストック → 生成済

#### ネタストック登録済みテーマ（10件）

カテゴリ・ターゲットが隣接しないよう並び順を設定済み。

| 並び | テーマカテゴリ | ターゲット |
|---|---|---|
| 1 | ドライアイ | 30代デスクワーカー |
| 2 | 老眼 | 40代・老眼世代 |
| 3 | 眼精疲労 | 女性全般 |
| 4 | 老眼 | 50代以上 |
| 5 | 子どもの視力 | 親御さん向け |
| 6 | 眼精疲労 | 30代デスクワーカー |
| 7 | 老眼 | 40代・老眼世代 |
| 8 | 肩こり・頭痛連動 | 50代以上 |
| 9 | 眼精疲労 | 女性全般 |
| 10 | 子どもの視力 | 親御さん向け |

#### 技術的な決定事項

- **APIキー管理**：`.env`ファイルで管理し、`.gitignore`でコミット対象外に設定。GitHub Actions実行時はSecrets（`NOTION_TOKEN`・`NOTION_DATABASE_ID`・`ANTHROPIC_API_KEY`）から注入する
- **記事生成方式**：`claude -p` CLI（subprocess経由）を使用。Anthropic Python SDKは不使用
- **Notion本文登録**：MarkdownをNotionブロックに変換して追記。100ブロック超は自動分割
- **出力クリーニング**：Claude出力から冒頭定型文〜末尾免責文の範囲のみを抽出する`clean_output()`で余分なコメントを除去
- **クエリのソート**：Notionの `created_time` 昇順でネタストックを取得（独自の並び順フィールドはDB未反映のため）
- **GitHubリポジトリ**：`https://github.com/tonkatsuphotos/eyecare-labo`（プッシュは `gh auth login` 後に実行予定）

---

### 2026-05-06

#### 作成・更新したファイル

| ファイル | 内容 |
|---|---|
| `tools/generate_topics.py` | 当月・翌月の季節感を考慮してネタストック10件を生成しNotionに一括登録するスクリプト |
| `tools/generate_image.py` | 最新記事を読み込みOpenAI gpt-image-1でサムネイル画像を生成・保存するスクリプト |
| `.github/workflows/generate_article.yml` | タイムアウト延長・ネタ生成・画像生成ステップの追加など複数更新 |

#### ワークフローの実行順序（更新後）

```
generate_topics.py → generate_article.py → generate_image.py → git commit & push
```

#### 技術的な決定事項

- **タイムアウト設定**：`subprocess.run` の `timeout` を 180秒 → 600秒に変更。ワークフローの `timeout-minutes` も 10 → 20 に延長
- **スケジュール変更**：ワークフローのcronを 2026-05-06 05:00 JST（= 2026-05-05 20:00 UTC）の1回限り実行に変更（`0 20 5 5 *`）
- **ネタ生成の重複回避**：`past_themes.md`（使用済みテーマ）とNotionの既存ネタストック一覧の両方をプロンプトに含めてClaudeに渡す
- **画像サイズ**：`1792x1024` はDALL-E 3専用。`gpt-image-1` の横長サポートサイズは `1536x1024` のためこちらを使用
- **画像保存先**：`images/YYYYMM_タイトル.png`（記事ファイル名のstemをそのまま流用）
- **画像APIコスト概算**：1枚あたり約$0.24（≒37円）。テキスト入力トークン＋出力画像6タイル（512×512換算）の合算
- **必要なGitHub Secret**：既存の `NOTION_TOKEN`・`NOTION_DATABASE_ID`・`ANTHROPIC_API_KEY` に加えて `OPENAI_API_KEY` を追加する必要あり